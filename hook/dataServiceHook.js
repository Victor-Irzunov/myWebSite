export const useDataService = async (link) => {
	let data;

	switch (link) {
		// case 'razrabotka-proekta-sanitarno-zashchitnoi-zoni':
		// 	const { dataService1 } = await import('@/constants/data/DataService');
		// 	data = dataService1;
		// 	break
	
	
	}

	return { data };
}